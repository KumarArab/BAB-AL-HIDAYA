---
import View from '../../views/ServiceDetail.astro';
import { serviceSlugs } from '../../data/services';
export const getStaticPaths = () => serviceSlugs.map((slug) => ({ params: { slug } }));
---
<View lang="en" slug={Astro.params.slug!} />
